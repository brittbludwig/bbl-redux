import sign from '$images/sign.png';

const Sign = () => {
  return (
    <div className="absolute bottom-0 left-[15%] w-50">
      <img src={sign} className="w-full" alt="sign" />
    </div>
  );
};

export default Sign;